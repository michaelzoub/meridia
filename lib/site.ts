/** X / Twitter profile for research and updates. */
export const SOCIAL_X_URL = "https://x.com/efimovcapital" as const;

/** Display handle shown in UI (profile URL may differ until migrated). */
export const SOCIAL_X_HANDLE_DISPLAY = "@meridia" as const;
